<?php

/*
 * eduVPN node configuration – single-server all-in-one.
 * Generated by Ansible – do not edit manually.
 *
 * On a single server the API is consumed on localhost.
 */

return [
    // Portal node API endpoint (use public FQDN over HTTPS).
    'apiUrl'       => 'https://{{ eduvpn_fqdn }}/vpn-user-portal/node-api.php',

    // This is always node 0 on a single-server setup.
    'nodeNumber'   => 0,

    // All profiles run on node 0.
    'profileIdList' => [{% for profile in profiles %}'{{ profile.id }}',{% endfor %}],
];
